import { ReactNode } from "react";

export interface ButtonType {
    className?: string;
    btnText?: ReactNode;
    btnWidth?: string;
    btnHeight?: string;
    btnBg?: string;
    btnColor?: string;
    btnBorder?: string;
    btnShadow?: string;
}